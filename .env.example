# Phase 1 always uses mock data. No credentials are required or consumed.
# These provider settings are placeholders for later phases.
LLM_API_KEY=
LLM_MODEL=
GITHUB_TOKEN=
GITHUB_MCP_COMMAND=
# Future transport adapter will define the argument format.
GITHUB_MCP_ARGS=

MAX_AGENT_STEPS=8
MAX_TOOL_RESULT_CHARS=30000
MAX_FILES_TO_INSPECT=20
MIN_FINDING_CONFIDENCE=0.75
LOG_LEVEL=info
